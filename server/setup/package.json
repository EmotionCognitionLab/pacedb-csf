{
  "name": "setup",
  "version": "1.0.0",
  "description": "Does one-time setup for the server-side elements of the HRV app.",
  "main": "setup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "node setup.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.184.0",
    "prompt": "^1.0.0",
    "serverless": "^1.26.0"
  }
}
